Biblioteka do obsługi api finsera [http://finser.pl]
======================================================

Instalacja:

  python setup.py install


Użycie: 

  Do pakietu dołączona jest prosta aplikacja konsolowa do obsługi finsera pod
linkusem obrazuje ona możliwości api. Po zainstalowaniu jest ona gotowa do
użycia. Aby odpalić aplikację wpisujemy w konsoli:

finser-client.py [OPTIONS]

Options:
  -h, --help            show this help message and exit
  -g GET, --get=GET     list last operations for given query
  -i INSERT, --insert=INSERT
                        insert given operation
  -r, --remove          remove last operation
  -l, --last            display last operations
  -a, --accounts        display accounts info
  -f, --force-login     force restore auth info

  Import Options:
    --get-loaders       list available loaders
    --import=LOAD       import file with specified loader
